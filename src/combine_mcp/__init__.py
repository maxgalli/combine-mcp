"""combine-mcp — retrieval MCP server for the CMS Combine corpus.

This ``__version__`` is the single source of truth for the package
version (pyproject.toml reads it via hatch's ``version.path``).
"""

__version__ = "0.2.0"
